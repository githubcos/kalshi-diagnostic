CURRENT PHASE: VERIFIED KALSHI PORT DEFECT — UNHEDGEABLE PARTIAL LEAD / RESIDUAL CLOSE.
LATEST VERIFIED EVIDENCE: after the hedgeability-v4 PAPER restart, the EC2 log still shows a carried PAIR with locked shares=0, unrealized residual P&L, and held time >8 minutes even though HedgeTimeout=20s. This state survived restart, so it is not ordinary quote waiting.
ROOT CAUSE VERIFIED IN SOURCE: OnPairArbSignal preflights hedgeability using the estimated lead size, but a Kalshi IOC/FAK lead may partially fill smaller. maybeRebalancePairArb then returns without action when the actual deficit is below the shared market-minimum hedge size. At timeout forceClosePairArb calls closePairArbResidual, but that function still contains the legacy Polymarket MinOrderShares early-return; a small Kalshi residual is therefore never sold and forceClose keeps failing because residual inventory remains.
PARITY INTENT: PolyArbPro lead->hedge semantics must never carry a directional residual that cannot form the second leg. This is a venue-compatibility correction only: after the actual lead fill, re-check hedgeability at the locked-profit limit and immediately unwind if a venue-valid hedge cannot be placed. Residual liquidation must not be blocked by the legacy Polymarket minimum-share guard.
ACTION QUEUED: apply control/patches/kalshi_unhedgeable_residual_v1.patch, gofmt strategy/trader.go, go test ./..., go build, restart PAPER :8085 under the existing hedgeability-v4 profile, refresh telemetry/status. LIVE_ALLOWED=false.
SAFETY: no credentials, signal thresholds, locked-profit math, or live enablement changed. Patch only closes impossible post-fill residual exposure sooner and removes a stale Polymarket-only residual-share gate from the Kalshi execution path.