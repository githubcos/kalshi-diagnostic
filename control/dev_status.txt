FOUND: restricted job kalshi-parity-evidence-publish-v2-20260825-1854 PASS. It refreshed the telemetry publisher, republished 40 live parity-source excerpts, and confirmed PAPER still listening on port 8085. No live trading was enabled.
VERIFIED DEFECT REMAINS: SETTLEMENT / MARK-TO-MARKET ACCOUNTING. PairArbPosition.markToMarket accepts a real NO price, but active pair-position callers still invoke markToMarket(currentYesPrice) only. The fallback synthesizes NO as round(1-YES), which is not authoritative Kalshi liquidation/mark value when YES and NO books have independent spreads/liquidity. Live evidence confirms affected calls in FormatPosition, PositionSnapshot and DashboardPositionSnapshot (both UnrealizedPnL and PairMarkToMarket).
PARTIAL-FILL EVIDENCE: the live source now defers matched inventory when authoritative fill price/quantity is unavailable, and uses order/fill reconciliation paths before recording shares. However stale Polymarket/CTF comments still exist around SizeMatched/fill cross-checking; these comments alone are not a defect and no partial-fill patch is justified yet.
NOT PATCHED YET: the correct mark-to-market fix requires authoritative current NO-price plumbing through the caller chain. Do not change strategy semantics, thresholds, entry, hedge timing, timeout logic or sizing just to remove the fallback. A wrong API-plumbing patch would be riskier than leaving PAPER-only accounting display imperfect.
EVIDENCE: latest forensic economics snapshot is still 2026-08-25T14:25:49Z: bankroll $19.8328 from $20.00, 33 lead attempts, 1 completed cycle, 0 hedge events, 1 timeout loss. This snapshot is too stale to support performance tuning. PAPER remains healthy on 8085.
ACTION: queue a fresh restricted evidence/status collection so the next run has current execution context and process health while PAPER continues gathering market data. No trading-code patch is forced this run because the defect is verified but the safe caller-plumbing implementation is not yet proven from available source context.
SAFETY: credentials untouched; LIVE hard-blocked; no PolyArbPro strategy semantics or thresholds changed.