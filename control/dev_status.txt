CURRENT PHASE: PAPER PARITY / VERIFIED CONFIGURATION-DRIFT CORRECTION.
LATEST VERIFIED STATE (2026-08-26 22:48Z): MODE=PAPER, LIVE_ORDERS_ALLOWED=false, process_present=true, port_8085_listening=true, PID=580985. Protected HTTP status endpoints still return 401/302; this is an auth-observability limitation only and credentials remain untouched.
VERIFIED DEFECT THIS RUN: active runtime PAPER config has drifted away from the last approved PolyArbPro-parity observation profile. Runtime telemetry now reports PairArbMinBTCGapUSD=30, PairArbMaxBTCGapUSD=40, PairArbMinTokenPrice=0.70, PairArbMaxTokenPrice=0.80, PairArbHedgeTimeoutSec=3, PairArbMaxHedgeDistanceCents=0, and PairArbUnprofitableAbortGraceSec=1. The approved parity profile documented after the positive-edge validation was min BTC gap=$1, max BTC gap=0/unbounded, token range=0.05-0.95, hedge timeout=8s, max hedge distance=1c, abort grace=2s, $5 size, 3c min locked profit, 1 slip tick, 2s max signal age, 1c stop loss, BOTH direction, FAK lead, dual pre-place. This is verified operating-profile drift, not a Kalshi venue defect, and it materially changes strategy admission semantics; this parity loop is not authorized to keep such tuning.
CURRENT PAPER FORENSICS: latest published live-trade monitor remains PASS with PAPER_8085=UP, blocker=none, lead=0, hedge=0, closed=0. It still confirms Kalshi inventory/cash-fee/locked-profit invariants and PAPER guard, but contains no execution sample proving a fill/accounting defect.
PARITY ASSESSMENT: no new Kalshi source/accounting defect is verified. The source-side lead-then-hedge compatibility correction remains appropriate. The only verified issue this run is runtime configuration drift away from the approved parity profile.
ACTION THIS RUN: restore ONLY the approved non-secret PAPER profile fields using a restricted config patch. Do not modify source strategy semantics, credentials, or live settings. Run BACKUP -> APPLY_PATCH -> GO_TEST ./... -> GO_BUILD -> START_PAPER -> UPDATE_TELEMETRY -> STATUS. Keep LIVE=false throughout. If the config drifts again after restore, treat the mutating optimizer/service as an observability/control defect and neutralize only that tuner, not trading semantics.
DEFECT GATES AFTER RESTORE: (1) fully matched pair with negative all-in locked P&L => verified execution/accounting defect; (2) authoritative fill quantity/cash-spend mismatch => verified Kalshi accounting defect; (3) residual/timeout loss => compare against intended PolyArbPro legging semantics before patching; (4) repeated profile drift after this explicit restore => verified control-plane defect requiring the smallest tuner-disable correction.
