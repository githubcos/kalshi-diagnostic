CURRENT PHASE: PAPER KALSHI FEED RECOVERED — EXECUTION/PARITY OBSERVATION.
VERIFIED RECOVERY: EC2 telemetry is live again, the agent is idle/healthy, PAPER is listening on :8085, and the current paper log shows nonzero Kalshi executable YES quotes (about 0.30–0.31) with nonzero book liquidity while BTC/Chainlink continue updating. The previous market-feed compatibility blocker is therefore no longer present in current paper evidence.
LATEST AGENT RESULT: kalshi-feed-rest-diagnostics-v1b-20260826-0917 PASS. GO_BUILD completed rc=0, PAPER restarted as pid 7623 on port 8085, STATUS passed before and after telemetry update, and LIVE_ALLOWED=false throughout.
PARITY CHECK: no new verified PolyArbPro strategy-semantic mismatch or accounting/execution defect is proven by the current session. Current-session lead_events=0, hedge_events=0, closed_events=0, so there is not yet authoritative fresh evidence to classify hedge timing, timeout/unwind, or completed-cycle P&L behavior.
SAFETY: PAPER mode remains enforced; live_orders_allowed=false. Credentials are untouched. Do not alter sizing, signal thresholds, hedge/timeout logic, settlement, accounting, or P&L semantics without a venue-specific defect proven by fresh Kalshi execution evidence.
OBSERVATION NOTE: docs/agent/live_trade_monitor reports PASS and PAPER_8085 up, but no execution-latency sample exists yet. The stale source_snapshot still does not prove which feed fallback source lines are deployed, so do not claim a new source patch from snapshot alone; runtime evidence is authoritative for current recovery state.
INFRA NOTE: docs/agent/local_error records one git fetch timeout at 09:40:20Z, but subsequent telemetry/monitor commits are updating normally and the agent heartbeat is idle/healthy, so this is treated as transient rather than a parity defect.
NEXT GATE: leave PAPER running and collect fresh lead/hedge/close events. Patch only if those events prove a Kalshi-port mismatch in partial fills, hedge timing, timeout/unwind, settlement, or accounting. If the feed drops again, capture the surfaced REST/WS error before changing code.