NEXT: settlement/mark-to-market parity now has a VERIFIED DEFECT. Active pair-position snapshot/display callers still invoke markToMarket(currentYesPrice) without authoritative Kalshi NO, causing fallback NO=round(1-YES). First inspect the republished live caller context from job kalshi-parity-evidence-publish-v2-20260825-1854 and identify the narrowest way to propagate current NO price through FormatPosition, PositionSnapshot and DashboardPositionSnapshot call chains. Do not alter signal, entry, sizing, hedge, timeout or strategy thresholds. Once caller plumbing is proven, create a restricted compatibility patch and job with backup -> APPLY_PATCH -> GOFMT -> GO_TEST -> GO_BUILD -> STATUS/COLLECT_AUDIT, keeping LIVE=false and PAPER on 8085. Then classify PARTIAL_FILL, HEDGE_TIMING and TIMEOUT_UNWIND from the remaining live evidence as VERIFIED_OK or DEFECT_PATCHED. After all source gates are resolved, obtain a fresh forensic economics snapshot newer than 2026-08-25T14:25:49Z and perform repeated PAPER validation. Credentials untouched; LIVE remains hard-blocked.