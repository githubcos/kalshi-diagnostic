NEXT: STOP treating repeated source-audit -> test -> build cycles as engineering progress. PAPER stays on port 8085 and LIVE remains hard-blocked. Complete the remaining PAIR-ARB Kalshi parity work as finite gates, in this exact order: (1) authoritative partial-fill quantity handling, (2) hedge admission/timing, (3) timeout/unwind semantics, (4) settlement semantics. Each gate must end in exactly VERIFIED_OK or DEFECT_PATCHED; raw symbol counts, CANDIDATE, and CONTINUE_OBSERVATION are not completion states. For every gate, inspect the actual execution context and record the decisive source locations/reasoning. A known explicit incomplete-parity marker already exists in PairArbPosition.markToMarket: legacy callers supplying only YES synthesize NO as 1-YES and the source itself says LIVE remains blocked until every caller carries Kalshi NO. Resolve/classify those callers as part of the finite audit. After all four gates: run gofmt only if code changed, then one full go test ./... and one go build; keep/restart PAPER only; produce a fresh post-patch forensic economics snapshot; separate signals from accepted/executed leads and report hedge events, timeout exits, fees, bankroll, and completed cycles. Then perform repeated paper validation and issue FINAL_PARITY_CERTIFIED only when all gates and evidence pass. Do not change strategy thresholds merely to create activity. Do not loop back to raw Polymarket symbol counting after a gate is resolved.