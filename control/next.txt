NEXT: CORRECT VERIFIED PAPER CONFIGURATION DRIFT, THEN RESUME OBSERVATION. Fresh runtime telemetry at 2026-08-26 22:48Z shows the bot alive on :8085 with LIVE_ALLOWED=false, but the active config has changed to min BTC gap=$30, max gap=$40, token range=0.70-0.80, hedge timeout=3s, max hedge distance=0, and abort grace=1s. Those values materially alter admission/legging semantics and do not match the last approved parity profile. Restore only these non-secret PAPER fields to the approved profile: min BTC gap=$1, max gap=0/unbounded, token range=0.05-0.95, hedge timeout=8s, max hedge distance=1c, abort grace=2s. Preserve $5 size, 3c min locked profit, 1 slip tick, 2s signal age, 1c stop loss, BOTH direction, FAK lead, dual pre-place, PAPER=true, and live hard-block. Use restricted BACKUP -> APPLY_PATCH -> GO_TEST ./... -> GO_BUILD -> START_PAPER -> UPDATE_TELEMETRY -> STATUS. Do not change credentials or source strategy semantics. After successful restore, leave PAPER running and verify the exact telemetry values. If these fields drift again without a parity-authorized job, classify that as a control-plane tuner defect and make the smallest correction needed to stop the tuner while leaving the trading engine and PAPER data collection intact. Continue execution/accounting defect gates: fully matched negative all-in locked P&L, authoritative fill/cash mismatch, or proven venue-specific timeout/unwind deviation.
